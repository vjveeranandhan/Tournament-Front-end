import React from 'react';
import './ClubManagement.css';

const ClubManagement = ({ title, text, imageUrl, buttonText }) => {
    return(
        <h2>Management</h2>
    );
}

export default ClubManagement